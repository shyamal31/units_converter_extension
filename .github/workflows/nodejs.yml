name: Node.js CI

on:
  push:
    branches: [updateReadme]
  pull_request:
    branches: [updateReadme]

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [21]

    steps:
      - name: Check out repository code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node-version }}

      - name: Install dependencies without audit
        run: npm install --no-audit

      - name: Run npm audit
        run: npm audit || true  # record audit but not affecr CI

      - name: Run tests
        run: npm test
      
      
